<Window
	x:Class="WadTool.Features.Dialogs.SelectSlot.SelectSlotWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:DarkUI.WPF.CustomControls;assembly=DarkUI.WPF"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:tombLib="clr-namespace:TombLib.WPF;assembly=TombLib.WPF"
	xmlns:vm="clr-namespace:WadTool.Features.Dialogs.SelectSlot"
	Title="{tombLib:Localize Title}"
	Width="406"
	Height="434"
	MinWidth="300"
	MinHeight="280"
	d:DataContext="{d:DesignInstance Type=vm:SelectSlotWindowViewModel}"
	Background="{DynamicResource Brush_Background}"
	ShowInTaskbar="False"
	Style="{DynamicResource CustomWindowStyle}"
	WindowStartupLocation="CenterOwner"
	mc:Ignorable="d">

	<controls:SpacedGrid
		Margin="{x:Static darkUI:Defaults.MediumThickness}"
		ColumnSpacing="{x:Static darkUI:Defaults.SmallThicknessWidth}"
		RowSpacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<Label
			Grid.Row="0"
			Grid.Column="0"
			VerticalAlignment="Center"
			Content="{tombLib:Localize ChosenId}"
			Target="{Binding ElementName=chosenIdNumericUpDown}" />
		<controls:NumericUpDown
			x:Name="chosenIdNumericUpDown"
			Grid.Row="0"
			Grid.Column="1"
			Width="100"
			HorizontalAlignment="Left"
			VerticalAlignment="Center"
			Increment="1"
			LargeIncrement="32"
			Maximum="4294967295"
			Minimum="0"
			Value="{Binding ChosenId, Mode=TwoWay}" />

		<Label
			Grid.Row="1"
			Grid.Column="0"
			VerticalAlignment="Center"
			Content="{tombLib:Localize SearchKeyword}"
			Target="{Binding ElementName=searchTextBox}" />
		<TextBox
			x:Name="searchTextBox"
			Grid.Row="1"
			Grid.Column="1"
			VerticalAlignment="Center"
			Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

		<ListBox
			x:Name="slotsList"
			Grid.Row="2"
			Grid.Column="0"
			Grid.ColumnSpan="2"
			DisplayMemberPath="DisplayName"
			ItemsSource="{Binding Slots}"
			MouseDoubleClick="OnSlotsListMouseDoubleClick"
			SelectedItem="{Binding SelectedSlot, Mode=TwoWay}" />

		<controls:SpacedStack
			Grid.Row="3"
			Grid.Column="0"
			Grid.ColumnSpan="2"
			HorizontalAlignment="Right"
			Orientation="Horizontal"
			Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
			<Button
				Width="80"
				Command="{Binding ConfirmCommand}"
				Content="{tombLib:Localize ~Common.OK}"
				IsDefault="True" />
			<Button
				Width="80"
				Command="{Binding CancelCommand}"
				Content="{tombLib:Localize ~Common.Cancel}"
				IsCancel="True" />
		</controls:SpacedStack>
	</controls:SpacedGrid>
</Window>
