<Window
	x:Class="TombEditor.Features.Dialogs.ImportedGeometryEditor.ImportedGeometryWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:DarkUI.WPF.CustomControls;assembly=DarkUI.WPF"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
	xmlns:local="clr-namespace:TombEditor.Features.Dialogs.ImportedGeometryEditor"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
	Title="Imported geometry"
	Width="860"
	Height="520"
	MinWidth="640"
	MinHeight="400"
	d:DataContext="{d:DesignInstance Type=local:ImportedGeometryWindowViewModel}"
	md:DialogServiceViews.IsRegistered="True"
	Background="{DynamicResource Brush_Background}"
	Style="{DynamicResource CustomWindowStyle}"
	WindowStartupLocation="CenterOwner"
	mc:Ignorable="d">

	<DockPanel Margin="{x:Static darkUI:Defaults.MediumThickness}" LastChildFill="True">

		<controls:SpacedStack
			DockPanel.Dock="Bottom"
			Margin="0,8,0,0"
			HorizontalAlignment="Right"
			Orientation="Horizontal"
			Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
			<Button Width="80" Command="{Binding OkCommand}" Content="OK" IsDefault="True" />
			<Button Width="80" Command="{Binding CancelCommand}" Content="Cancel" IsCancel="True" />
		</controls:SpacedStack>

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="8" />
				<ColumnDefinition Width="260" MinWidth="220" />
			</Grid.ColumnDefinitions>

			<!--  Geometry list  -->
			<DockPanel Grid.Column="0">
				<controls:SpacedStack DockPanel.Dock="Top" Margin="0,0,0,6" Orientation="Horizontal" Spacing="{x:Static darkUI:Defaults.SmallThicknessWidth}">
					<Button Width="70" Command="{Binding AddImportedGeometryCommand}" Content="Add..." />
					<Button Width="70" Command="{Binding DeleteImportedGeometryCommand}" Content="Delete" />
					<Button Width="70" Command="{Binding AssignCommand}" Content="Assign" />
				</controls:SpacedStack>
				<DataGrid
					AutoGenerateColumns="False"
					CanUserAddRows="False"
					HeadersVisibility="Column"
					ItemsSource="{Binding Geometries}"
					SelectedItem="{Binding SelectedGeometry, Mode=TwoWay}">
					<DataGrid.Columns>
						<DataGridTextColumn Width="120" Binding="{Binding Name}" Header="Name" IsReadOnly="True" />
						<DataGridTemplateColumn Width="32">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Button Margin="1" Command="{Binding BrowseCommand}" Content="..." />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
						<DataGridTextColumn Width="*" Binding="{Binding Path, UpdateSourceTrigger=LostFocus}" Header="Path" />
						<DataGridTextColumn Width="60" Binding="{Binding Scale, UpdateSourceTrigger=LostFocus}" Header="Scale" />
						<DataGridTextColumn Width="140" Binding="{Binding ErrorMessage}" Header="Status" IsReadOnly="True" />
					</DataGrid.Columns>
				</DataGrid>
			</DockPanel>

			<GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch" />

			<!--  Instance properties  -->
			<StackPanel Grid.Column="2">
				<TextBlock Margin="0,0,0,2" Text="Assigned model:" />
				<TextBlock Margin="0,0,0,8" Text="{Binding AssignedModelDisplay}" TextWrapping="Wrap" />

				<TextBlock Margin="0,0,0,2" Text="Lighting model:" />
				<ComboBox Margin="0,0,0,8" ItemsSource="{Binding LightingModels}" SelectedItem="{Binding LightingModel, Mode=TwoWay}" />

				<TextBlock Margin="0,0,0,2" Text="Color:" />
				<Button
					Width="120"
					Height="22"
					Margin="0,0,0,8"
					HorizontalAlignment="Left"
					Command="{Binding PickColorCommand}">
					<Button.Background>
						<SolidColorBrush Color="{Binding Color}" />
					</Button.Background>
				</Button>

				<CheckBox Margin="0,3" Content="Sharp edges" IsChecked="{Binding SharpEdges, Mode=TwoWay}" />
				<CheckBox Margin="0,3" Content="Hidden" IsChecked="{Binding Hidden, Mode=TwoWay}" />
				<CheckBox Margin="0,3" Content="Use alpha test instead of alpha blend" IsChecked="{Binding UseAlphaTest, Mode=TwoWay}" IsEnabled="{Binding IsTombEngine}" />

				<Button Margin="0,12,0,0" Command="{Binding EditMaterialsCommand}" Content="Edit materials..." />
			</StackPanel>
		</Grid>
	</DockPanel>
</Window>
