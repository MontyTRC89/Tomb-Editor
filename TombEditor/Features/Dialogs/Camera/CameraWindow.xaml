<Window
	x:Class="TombEditor.Features.Dialogs.Camera.CameraWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:DarkUI.WPF.CustomControls;assembly=DarkUI.WPF"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
	xmlns:vm="clr-namespace:TombEditor.Features.Dialogs.Camera"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
	xmlns:tombLib="clr-namespace:TombLib.WPF;assembly=TombLib.WPF"
	Title="{tombLib:Localize Title}"
	d:DataContext="{d:DesignInstance Type=vm:CameraWindowViewModel}"
	md:DialogServiceViews.IsRegistered="True"
	Background="{DynamicResource Brush_Background}"
	ResizeMode="NoResize"
	ShowInTaskbar="False"
	SizeToContent="WidthAndHeight"
	Style="{DynamicResource CustomWindowStyle}"
	WindowStartupLocation="CenterOwner"
	mc:Ignorable="d">

	<controls:SpacedGrid
		MinWidth="240"
		Margin="{x:Static darkUI:Defaults.MediumThickness}"
		ColumnSpacing="{x:Static darkUI:Defaults.SmallThicknessWidth}"
		RowSpacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="80" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<!--  Glide timer  -->
		<Label
			Grid.Row="0"
			Grid.Column="0"
			VerticalAlignment="Center"
			Content="{tombLib:Localize GlideTimer}" />
		<controls:NumericUpDown
			Grid.Row="0"
			Grid.Column="1"
			VerticalAlignment="Center"
			Increment="1"
			IsEnabled="{Binding IsMoveTimerEnabled}"
			Maximum="31"
			Value="{Binding MoveTimer, Mode=TwoWay}" />
		<CheckBox
			Grid.Row="0"
			Grid.Column="2"
			VerticalAlignment="Center"
			Content="{tombLib:Localize GlideOut}"
			IsChecked="{Binding GlideOut, Mode=TwoWay}"
			IsEnabled="{Binding IsGlideOutEnabled}" />

		<!--  Camera mode  -->
		<Label
			Grid.Row="1"
			Grid.Column="0"
			VerticalAlignment="Center"
			Content="{tombLib:Localize Mode}" />
		<ComboBox
			Grid.Row="1"
			Grid.Column="1"
			Grid.ColumnSpan="2"
			VerticalAlignment="Center"
			DisplayMemberPath="DisplayName"
			ItemsSource="{Binding Modes}"
			SelectedItem="{Binding SelectedMode, Mode=TwoWay}" />

		<!--  Buttons  -->
		<controls:SpacedStack
			Grid.Row="2"
			Grid.Column="0"
			Grid.ColumnSpan="3"
			HorizontalAlignment="Right"
			Orientation="Horizontal"
			Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
			<Button
				Width="80"
				Command="{Binding ConfirmCommand}"
				Content="{tombLib:Localize ~Common.OK}"
				IsDefault="True" />
			<Button
				Width="80"
				Command="{Binding CancelCommand}"
				Content="{tombLib:Localize ~Common.Cancel}"
				IsCancel="True" />
		</controls:SpacedStack>
	</controls:SpacedGrid>
</Window>
