<UserControl
	x:Class="TombIDE.ScriptingStudio.Shell.RootShellView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:shell="clr-namespace:TombIDE.ScriptingStudio.Shell"
	d:DataContext="{d:DesignInstance Type=shell:RootShellViewModel,
	                                 IsDesignTimeCreatable=False}"
	mc:Ignorable="d">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<ContentControl Content="{Binding MenuView}" />

		<ContentControl
			Grid.Row="1"
			Content="{Binding ToolBarView}"
			Visibility="{Binding IsToolStripVisible, Converter={StaticResource BoolToVisibilityConverter}}" />

		<ContentControl Grid.Row="2" Content="{Binding WorkbenchView}" />

		<ContentControl
			Grid.Row="3"
			Content="{Binding StatusBarView}"
			Visibility="{Binding IsStatusStripVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
	</Grid>
</UserControl>
