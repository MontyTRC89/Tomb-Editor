<Window
	x:Class="TombIDE.Forms.EngineHelpWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:DarkUI.WPF.CustomControls;assembly=DarkUI.WPF"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="Engine Help"
	Width="820"
	Height="620"
	MinWidth="600"
	MinHeight="300"
	Style="{DynamicResource CustomWindowStyle}"
	WindowStartupLocation="CenterScreen"
	mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<ScrollViewer VerticalScrollBarVisibility="Auto">
			<controls:SpacedStack Margin="{x:Static darkUI:Defaults.JumboThickness}" Spacing="{x:Static darkUI:Defaults.JumboThicknessWidth}">
				<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.H1FontSize}"
						FontWeight="Bold"
						Text="Tomb Raider 1 (TR1X) / Tomb Raider 2 (TR2X)" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.SmallFontSize}" Text="by: Lost Artefacts Team" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.LargeFontSize}" TextWrapping="Wrap">
						These are enhanced engines for Tomb Raider 1 and 2, offering smooth, true 60 FPS gameplay and expanded creative tools.
						They preserve the original feel while adding flexibility through full game-flow scripting in a simple JSON format,
						and an injection system that unlocks new features and removes engine limitations.
					</TextBlock>
				</controls:SpacedStack>

				<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.H1FontSize}"
						FontWeight="Bold"
						Text="Tomb Raider 2 (TR2Main) / Tomb Raider 3 (tomb3)" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.SmallFontSize}" Text="by: Arsunt (TR2Main) and Troye (tomb3)" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.LargeFontSize}" TextWrapping="Wrap">
						These are the original Tomb Raider 2 and 3 engines with numerous bug fixes and restored features from the console versions.
						From lighting to UI elements and gameplay details, they bring back lost aspects which are fully customizable by the players.
						Both engines utilize a game-flow scripting language for simple gameplay tweaks, while keeping the experience authentic.
					</TextBlock>
				</controls:SpacedStack>

				<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.H1FontSize}"
						FontWeight="Bold"
						Text="Tomb Raider 4 (Original TRLE)" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.SmallFontSize}" Text="by: Core Design" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.LargeFontSize}" TextWrapping="Wrap">
						This is the original, unmodified Tomb Raider 4 engine, exactly as it was released with the original TRLE editor.
						Perfect for creators seeking to build custom levels within the classic, traditional framework.
					</TextBlock>
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.SmallFontSize}"
						Foreground="{DynamicResource Brush_Text_Warning}"
						TextWrapping="Wrap">
						Warning: This engine has never received any updates since its original release. It may not function correctly on modern systems.
					</TextBlock>
				</controls:SpacedStack>

				<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.H1FontSize}"
						FontWeight="Bold"
						Text="Tomb Raider Next-Generation" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.SmallFontSize}" Text="by: Paolone" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.LargeFontSize}" TextWrapping="Wrap">
						This enhanced Tomb Raider 4 engine, featuring the Next-Generation addon, expands creative possibilities
						with advanced scripting tools and a plugin system. It gives level builders greater control over gameplay mechanics,
						allowing for complex interactions, custom events, and deeper game logic customization.
					</TextBlock>
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.SmallFontSize}"
						Foreground="{DynamicResource Brush_Text_Highlight}"
						TextWrapping="Wrap">
						Note: Some anti-virus software, including Windows Defender, may flag this engine as a false positive.
						To prevent this, add the project directory to your anti-virus exceptions list.
					</TextBlock>
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.SmallFontSize}"
						Foreground="{DynamicResource Brush_Text_Warning}"
						TextWrapping="Wrap">
						Warning: This engine is known to not function correctly on modern systems.
						External software, such as dgVoodoo, might be required for proper operation.
					</TextBlock>
				</controls:SpacedStack>

				<!--<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.H1FontSize}"
						FontWeight="Bold"
						Text="FLEP (FLExible Patcher)" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.LargeFontSize}" TextWrapping="Wrap">
						FLEP is a specialized tool for the "Tomb Raider Next-Generation" preset,
						allowing level builders to modify hardcoded values, fix engine bugs,
						and adjust gameplay mechanics to their liking.
					</TextBlock>
				</controls:SpacedStack>-->

				<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.MediumThicknessWidth}">
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.H1FontSize}"
						FontWeight="Bold"
						Text="Tomb Engine" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.SmallFontSize}" Text="by: The Tomb Engine Team" />
					<TextBlock FontSize="{x:Static darkUI:Defaults.LargeFontSize}" TextWrapping="Wrap">
						Tomb Engine is a powerful and flexible engine with modern enhancements, including 60 FPS rendering, SSAO, dynamic shadows, and more.
						Built for both experienced developers and newcomers, it offers advanced features while remaining easy to use.
						With Lua programming support and an intuitive node system, it allows for deep customization and creativity.
					</TextBlock>
					<TextBlock
						FontSize="{x:Static darkUI:Defaults.SmallFontSize}"
						Foreground="{DynamicResource Brush_Text_Highlight}"
						TextWrapping="Wrap">
						Note: This engine is actively being developed, and changes to the scripting API may occur.
					</TextBlock>
				</controls:SpacedStack>
			</controls:SpacedStack>
		</ScrollViewer>

		<Border
			Grid.Row="1"
			Padding="{x:Static darkUI:Defaults.LargeThickness}"
			Background="{DynamicResource Brush_Background_Alternative}">
			<Button
				Width="{x:Static darkUI:Defaults.StandardButtonWidth}"
				HorizontalAlignment="Right"
				Click="Button_Click"
				Content="OK" />
		</Border>
	</Grid>
</Window>
